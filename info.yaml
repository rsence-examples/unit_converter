
# The human-readable product name of the package
title: Unit Converter

# The human-readable version of the package
version: 1.0.0

# A brief description of the package (rdoc formatting supported)
description: | Unit converter example. Demonstrates the usage of YAML for UI coding and binding of values between the client and the server.

# System version requirement.
sys_version: '>= 2.0.0'